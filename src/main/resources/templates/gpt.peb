<!DOCTYPE html>
<html>
<head>
    <title>GPT Response</title>
    <style>
        /* 針對整個容器的樣式 */
        .info-container {
            font-family: Arial, sans-serif; /* 調整字體 */
            margin: 20px; /* 增加外部間距 */
            padding: 20px; /* 增加內部填充 */
            border: 1px solid #ddd; /* 增加邊框 */
            border-radius: 8px; /* 邊角圓滑 */
            background-color: #f9f9f9; /* 背景顏色 */
        }

        /* 調整段落的間距 */
        .info-container p {
            margin-bottom: 10px; /* 段落之間的間距 */
            font-size: 16px; /* 調整字體大小 */
        }

        /* 鏈接容器 */
        .link-container {
            margin-top: 20px; /* 增加與上方信息的距離 */
        }
        .response-container {
            border-radius: 8px; /* 讓邊角變圓潤 */
            border: 1px solid #aaa; /* 較柔和的灰色邊框 */
            padding: 15px; /* 更寬敞的內邊距 */
            margin: 20px 0; /* 讓區塊與上下元素保持適當距離 */
            background-color: #f0f0f0; /* 較柔和的淺灰背景 */
            color: #333; /* 深灰色的文字，避免太強烈的對比 */
            font-family: Arial, sans-serif; /* 清晰的字體樣式 */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* 添加輕微的陰影來增加立體感 */
        }
        .response-container p {
            margin: 0; /* 確保文字與容器內邊距一致 */
            line-height: 1.6; /* 讓文字行間距更加舒適 */
        }
    </style>
</head>
<body>
    <h1>GPT Response</h1>
    <div class="info-container">
    <p>Status: {{ status }}</p>
    <div class="response-container">
        <p>{{ aiResponse | raw }}</p>
    </div>
    <p>Current time: {{ current }}</p>
    </div>
</body>
</html>